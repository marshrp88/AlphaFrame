# UX Tokens

Purpose (10th grade): A small set of colors, spacing, and text sizes used across the app so everything looks consistent.

Procedure:
- Tokens live in `styles/tokens.css`.
- Use utilities like `.card`, `.btn-primary`, and variables like `var(--space-4)`.

Conclusion: Faster, cleaner UI changes with one source of truth.
